package com.JustAlo;

import com.JustAlo.Entity.RequestOffers;
import org.springframework.data.jpa.repository.JpaRepository;

public interface RequestOffersRepository extends JpaRepository<RequestOffers, Long> {
}